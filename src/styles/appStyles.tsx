import { StyleSheet } from 'react-native';

export const appStyles = StyleSheet.create({
  ghMargin: {
    marginHorizontal: 20,
  },
  backgroundPokeball: {
    position: 'absolute',
    top: -100,
    right: -100,
    width: 300,
    height: 300,
    opacity: 0.3,
  },
  title: {
    fontSize: 35,
    fontWeight: 'bold',
  },
});
